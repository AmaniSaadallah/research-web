/**
 * Theme configuration for the application
 * Defines color modes and default settings
 */

const theme = {
  config: {
    initialColorMode: 'light',
    useSystemColorMode: false,
  }
};

export default theme;